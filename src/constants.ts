import * as os from "os";
import * as path from "path";
import * as vscode from "vscode";

export const extensionPrefix: string = 'azure';
export const severityFilterSetting: string = 'resourceFilter';
